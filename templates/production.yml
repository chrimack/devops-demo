jobs:
  - job: Build
    displayName: Build app
    steps:
      - template: steps.yml
        parameters:
          tag: production
          loop: for i in {1..10}; do echo "loading..."; sleep .5; done;
